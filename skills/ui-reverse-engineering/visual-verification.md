# Visual Verification — Step 8 + 9

> **This document has been moved to the `visual-debug` skill.**
> Read `visual-debug/verification.md` for the full verification procedure (Phase A/B/C/D/H/E).
>
> For quick comparison (standalone): use `visual-debug/SKILL.md` scripts (`batch-scroll.sh`, `batch-compare.sh`).
>
> The content is identical — all phases, gates, self-healing loop, and completion criteria are preserved.
